import React from "react";
import styled from "styled-components";
const SearchUnstyled = React.memo(() => {
    return (
        <>search</>
    )
})

export const Search = styled(SearchUnstyled)`

`